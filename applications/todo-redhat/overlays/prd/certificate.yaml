apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: todo-redhat-prd
spec:
  secretName: todo-redhat-prd-tls

  issuerRef:
    kind: ClusterIssuer
    name: ca-cluster-issue-letsencrypt

  commonName: todo.hclareth.space

  dnsNames:
    - todo.hclareth.space