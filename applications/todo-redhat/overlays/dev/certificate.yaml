apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: todo-redhat-dev
spec:
  secretName: todo-redhat-dev-tls

  issuerRef:
    kind: ClusterIssuer
    name: ca-cluster-issue-letsencrypt

  commonName: todo-dev.hclareth.space

  dnsNames:
    - todo-dev.hclareth.space