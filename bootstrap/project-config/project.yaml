apiVersion: config.openshift.io/v1
kind: Project
metadata:
  name: cluster
spec:
  projectRequestTemplate:
    name: corporate-project-template
  projectRequestMessage: "This project was created by the HNL7 project template."