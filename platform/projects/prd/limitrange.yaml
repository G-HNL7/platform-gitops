apiVersion: v1
kind: LimitRange
metadata:
  name: resource-quota-prd
  namespace: prd
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 500m
        memory: 512Mi

      default:
        cpu: "2"
        memory: 2Gi

      max:
        cpu: "8"
        memory: 16Gi
